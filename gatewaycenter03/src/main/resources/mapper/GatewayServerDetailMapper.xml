<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.melo.gatewaycenter.infrastructure.mapper.GatewayServerDetailMapper">

    <resultMap id="gatewayServerDetailMap" type="com.melo.gatewaycenter.infrastructure.po.GatewayServerDetail">
        <id column="id" property="id"/>
        <id column="group_id" property="groupId"/>
        <id column="gateway_id" property="gatewayId"/>
        <id column="gateway_name" property="gatewayName"/>
        <id column="gateway_address" property="gatewayAddress"/>
        <id column="status" property="status"/>
        <id column="create_time" property="createTime"/>
        <id column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.melo.gatewaycenter.infrastructure.po.GatewayServerDetail">
        INSERT INTO gateway_server_detail(group_id, gateway_id, gateway_name, gateway_address, status, create_time, update_time)
        VALUES (#{groupId}, #{gatewayId}, #{gatewayName}, #{gatewayAddress}, #{status}, NOW(), NOW());
    </insert>

    <select id="query" parameterType="java.lang.String" resultMap="gatewayServerDetailMap">
        SELECT gateway_id, gateway_name, gateway_address, status
        FROM gateway_server_detail
        WHERE gateway_id = #{gatewayId} AND gateway_address = #{gatewayAddress}
    </select>

    <update id="updateStatus" parameterType="com.melo.gatewaycenter.infrastructure.po.GatewayServerDetail">
        UPDATE gateway_server_detail
        SET status = #{status}
        WHERE gateway_id = #{gatewayId} AND gateway_address = #{gatewayAddress}
    </update>

</mapper>
